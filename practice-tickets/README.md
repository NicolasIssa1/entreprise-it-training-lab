# Practice Tickets

Reserved for future expansion of the fake ticket bank beyond the initial set built
into the Ticket Simulator app (`dhl-training-hub/src/lib/data/tickets.ts`). All
tickets are fake/generic training data — see `CLAUDE.md`.
