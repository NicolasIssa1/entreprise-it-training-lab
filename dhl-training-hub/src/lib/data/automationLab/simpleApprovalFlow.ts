import { AutomationLabScenario } from "@/lib/types";

// Automation Lab practice exercise 2. Fictional data — see root CLAUDE.md.
export const simpleApprovalFlowScenario: AutomationLabScenario = {
  id: "simple-approval-flow",
  title: "Build a Simple Approval Flow",
  description: "A short exercise in the very common approve/reject pattern behind expense requests, purchase requests, and more.",
  isEnterpriseProject: false,
  difficulty: "Foundation",
  estimatedMinutes: 8,
  scenarioBrief:
    "A team submits expense requests to a SharePoint list. Requests over a certain amount need manager approval before they're processed. Build a workflow that starts an approval when needed, records the outcome, and lets the requester know what happened.",
  toolsInvolved: ["Power Automate", "SharePoint", "Outlook"],
  relatedTopicIds: ["approvals-and-notifications", "conditions-and-branching", "sharepoint-fundamentals"],
  likelyTeams: ["applications"],
  learningObjectives: [
    "Practice the approve/reject pattern that underlies many enterprise workflows",
    "Use a condition to branch based on a threshold value",
    "Recognize why skipping a timed-out approval automatically is a risky shortcut",
  ],
  blocks: [
    { id: "trigger-item-created", category: "trigger", role: "essential", idealPosition: 1,
      label: "Trigger: when a new expense request item is created",
      description: "Starts the flow the moment a request is submitted to the list.",
      feedback: "Correct — approvals should react to a real submission, not run on a fixed schedule." },
    { id: "trigger-scheduled", category: "trigger", role: "distractor",
      label: "Scheduled trigger: every day at 17:00",
      description: "Only checks for new requests once a day, at a fixed time.",
      feedback: "This would delay every approval request until the end of the day, even ones submitted first thing in the morning — not a good fit for something that should start right away." },
    { id: "condition-amount", category: "condition", role: "essential", idealPosition: 2,
      label: "Condition: amount > $500 (requires manager approval)",
      description: "Only routes requests above the threshold to an approver.",
      feedback: "Correct — this is exactly the branching logic described in the brief." },
    { id: "condition-department-finance", category: "condition", role: "distractor",
      label: "Condition: requester department = Finance",
      description: "Only continues for requests from the Finance department.",
      feedback: "The brief's threshold is about amount, not department — this filter doesn't match the actual rule described." },
    { id: "action-start-approval", category: "action", role: "essential", idealPosition: 3,
      label: "Start an approval request to the requester's manager",
      description: "Sends the request to the right person for a decision.",
      feedback: "Correct — this is the core approval step the workflow exists for." },
    { id: "action-record-outcome", category: "action", role: "essential", idealPosition: 4,
      label: "Record the approval outcome (approved/rejected) on the list item",
      description: "Updates the request's status once a decision is made.",
      feedback: "Correct — keeps the outcome tracked on the record itself, not just in an email somewhere." },
    { id: "action-notify-requester", category: "action", role: "essential", idealPosition: 5,
      label: "Notify the requester of the outcome",
      description: "Lets the person who submitted the request know what happened.",
      feedback: "Correct — matches the brief's \"lets the requester know\" requirement." },
    { id: "logic-timeout-escalation", category: "logic", role: "recommended", idealPosition: 6,
      label: "Error handling: if the manager doesn't respond within 3 days, escalate to their manager",
      description: "Prevents a request from sitting unanswered indefinitely.",
      feedback: "Good practice — without this, a request can simply stall forever if the approver misses it or is away." },
    { id: "action-auto-approve-timeout", category: "action", role: "distractor",
      label: "Automatically approve if no response within 1 hour",
      description: "Approves the request by default if the manager hasn't responded quickly.",
      feedback: "This defeats the purpose of requiring approval in the first place — a missed notification would silently turn into an approval, which is the opposite of what an approval step is for." },
  ],
  modelWorkflowSummary:
    "A trigger fires when a new expense request is created, a condition checks whether the amount is over the threshold, and if so the flow starts an approval with the requester's manager, records the outcome on the list item, and notifies the requester — with a timeout escalation if the manager doesn't respond, but never an automatic approval on timeout.",
};
