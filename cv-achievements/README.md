# CV Achievements

Reserved for a future export/archive of CV Achievement Tracker entries (see the app's
CV Tracker page) once the internship ends and CV bullets are finalized. Entries are
tracked in-app via `localStorage` in Version 1 — see `CLAUDE.md` for the honesty rule
on involvement levels.
